services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - path: .env.local
        required: false
    volumes:
      - trace-data:/data
    restart: unless-stopped

volumes:
  trace-data:
